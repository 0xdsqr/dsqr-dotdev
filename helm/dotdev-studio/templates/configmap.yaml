apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dotdev-studio.fullname" . }}-config
  labels:
    {{- include "dotdev-studio.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
