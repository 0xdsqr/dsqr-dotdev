apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dotdev-labs.fullname" . }}-config
  labels:
    {{- include "dotdev-labs.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
