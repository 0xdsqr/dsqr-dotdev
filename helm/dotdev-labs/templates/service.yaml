apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotdev-labs.fullname" . }}
  labels:
    {{- include "dotdev-labs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dotdev-labs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
