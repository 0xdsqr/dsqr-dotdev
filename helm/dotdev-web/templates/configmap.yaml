apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dotdev-web.fullname" . }}-config
  labels:
    {{- include "dotdev-web.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
