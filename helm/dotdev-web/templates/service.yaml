apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotdev-web.fullname" . }}
  labels:
    {{- include "dotdev-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dotdev-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
