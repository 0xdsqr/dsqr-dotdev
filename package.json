{
  "name": "dsqr-dotdev",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "all": "bun run clean && bun run build",
    "build": "bun run --filter='@dsqr-dotdev/db' build && bun run --filter='@dsqr-dotdev/core' build && bun run --filter='dotdev' build && bun run --filter='studio' build",
    "clean": "rm -rf packages/*/dist packages/*/coverage apps/*/dist apps/*/.output",
    "dev:studio": "bun run --filter='studio' dev",
    "dev:dotdev": "bun run --filter='dotdev' dev",
    "style:fix": "nix fmt .",
    "typecheck": "bun run --filter '*' typecheck",
    "sync:check": "syncpack list-mismatches",
    "sync:fix": "syncpack fix-mismatches"
  },
  "workspaces": [
    "apps/dotdev",
    "apps/studio",
    "packages/blog-post",
    "packages/core",
    "packages/db",
    "packages/ui",
    "tooling/build",
    "tooling/typescript"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "syncpack": "^13.0.4"
  },
  "peerDependencies": {
    "typescript": "^5.8.0"
  },
  "private": true
}
