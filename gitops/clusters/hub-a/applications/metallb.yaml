# Generated by `nix run .#gitopsGenerateApplications`.
# Prefer cluster-specific overrides as Kustomize patches in this directory's kustomization.yaml.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    homelab.dev/chart: metallb
    homelab.dev/chart-repository: https://metallb.github.io/metallb
    homelab.dev/chart-version: 0.15.3
    homelab.dev/values-common-file: gitops/manifests/metallb/base/values-common.yaml
    homelab.dev/values-overlay-file: gitops/manifests/metallb/overlays/hub-a/values-overrides.yaml
  labels:
    app.kubernetes.io/instance: metallb
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: metallb
    app.kubernetes.io/part-of: cluster-networking
    app.kubernetes.io/version: 0.15.3
    homelab.dev/owner: platform
    homelab.dev/tier: platform-addon
spec:
  project: platform
  sources:
    - repoURL: https://metallb.github.io/metallb
      chart: metallb
      targetRevision: 0.15.3
      helm:
        releaseName: metallb
        valueFiles:
          - $values/gitops/manifests/metallb/base/values-common.yaml
          - $values/gitops/manifests/metallb/overlays/hub-a/values-overrides.yaml
    - repoURL: https://github.com/0xdsqr/dsqr-dotdev.git
      targetRevision: master
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: metallb-system
  syncPolicy:
    automated:
      enabled: false
      prune: false
      selfHeal: false
    managedNamespaceMetadata:
      labels:
        homelab.dev/owner: platform
        homelab.dev/tier: platform
    retry:
      limit: 3
      refresh: true
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
