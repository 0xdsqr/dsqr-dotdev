# Generated by `nix run .#gitopsGenerateApplications`.
# Cluster-scoped namespaces and workload AppProjects reconcile before platform add-ons.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-foundation
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    homelab.dev/ownership-handoff: root-to-child
  labels:
    app.kubernetes.io/instance: cluster-foundation
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: cluster-foundation
    app.kubernetes.io/part-of: dsqr-gitops
    homelab.dev/owner: platform
    homelab.dev/tier: gitops
spec:
  project: bootstrap
  source:
    repoURL: https://github.com/0xdsqr/dsqr-dotdev.git
    targetRevision: refs/heads/master
    path: gitops/manifests/cluster-foundation/overlays/hub-a
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    retry:
      limit: 5
      refresh: true
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 3m
    # Migration-only exception: these resources are currently tracked by the root hub-a
    # Application. Enable FailOnSharedResource immediately after this child adopts them.
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
