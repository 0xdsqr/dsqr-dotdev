# Generated by `nix run .#gitopsGenerateApplications`.
# Metrics Server is intentionally manual during platform adoption.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metrics-server
  namespace: ${ARGOCD_NAMESPACE}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    homelab.dev/chart: metrics-server
    homelab.dev/chart-repository: https://kubernetes-sigs.github.io/metrics-server/
    homelab.dev/chart-version: 3.13.0
    homelab.dev/values-common-file: ${VALUES_COMMON_FILE}
    homelab.dev/values-overlay-file: ${VALUES_OVERLAY_FILE}
  labels:
    app.kubernetes.io/instance: metrics-server
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: metrics-server
    app.kubernetes.io/part-of: cluster-observability
    app.kubernetes.io/version: 0.8.0
    homelab.dev/owner: platform
    homelab.dev/tier: platform-addon
spec:
  project: platform
  sources:
    - repoURL: https://kubernetes-sigs.github.io/metrics-server/
      chart: metrics-server
      targetRevision: 3.13.0
      helm:
        releaseName: metrics-server
        valueFiles:
          - ${VALUES_REF}/${VALUES_COMMON_FILE}
          - ${VALUES_REF}/${VALUES_OVERLAY_FILE}
    - repoURL: ${VALUES_REPO_URL}
      targetRevision: ${VALUES_TARGET_REVISION}
      ref: values
  destination:
    server: ${DESTINATION_SERVER}
    namespace: kube-system
  syncPolicy:
    automated:
      enabled: false
      prune: false
      selfHeal: false
    retry:
      limit: 3
      refresh: true
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - ApplyOutOfSyncOnly=true
