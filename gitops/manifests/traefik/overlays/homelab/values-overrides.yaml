# Homelab-only Helm value overrides for Traefik.
# Use this for cluster-specific service, ingress, dashboard, node placement, or pod metadata settings.
# Leave empty when homelab uses the base values unchanged.
{}
