# Homelab-only Helm value overrides for Cilium.
# Use this for cluster-specific API server, networking, rollout, node placement, or pod metadata settings.
# Leave empty when homelab uses the base values unchanged.
{}
