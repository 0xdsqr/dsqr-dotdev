# Homelab-only Helm value overrides for k8s-monitoring.
# Use this for cluster-specific observability destinations, collectors, resources, node placement, or pod metadata.
# Leave empty when homelab uses the base values unchanged.
{}
